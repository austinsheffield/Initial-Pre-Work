//Learned to import a default export. Unlike exported values, the primary method of importing a default export is to write the value's name after the import.
"use strict";
import subtract from "math_functions";
subtract(7,4);
