//Learned to create random fractions with Math.randowm(). This will generate a random decimal number between 0 and up to 1. Can return a 0 but cannot return 1.
//Can return a random number with return.
function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
