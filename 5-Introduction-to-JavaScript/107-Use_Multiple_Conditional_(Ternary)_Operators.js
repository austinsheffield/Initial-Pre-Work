//Learned to use multiple conditional operators.
//Note that for the last operator you should not put any other conditions.
function checkSign(num) {
  return num == 0 ? "zero" : num > 0 ? "positive" : "negative";
}

checkSign(10);
