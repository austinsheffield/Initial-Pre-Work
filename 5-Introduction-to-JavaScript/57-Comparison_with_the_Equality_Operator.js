//Learned to compare values with tht equality operator to symplify.
//== does not equal =. == compares values and returns true or false, whereas = assigns values.
// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(12);
