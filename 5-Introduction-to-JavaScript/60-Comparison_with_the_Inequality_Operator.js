//Learned about the inequality operator (!=) and how to use it. It returns true if false and false if true. Doesnt care what data type of value.
//Think of it like a computer thinks of it: 1 = on = true / and / 0 = off = false
// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);
