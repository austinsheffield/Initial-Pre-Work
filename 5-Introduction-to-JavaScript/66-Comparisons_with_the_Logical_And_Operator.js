// Learned to use the logical and operator (&&) to simplify from two if statements to one. This allows you to compare two different if statements.
function testLogicalAnd(val) {
  // Only change code below this line

  if (val <= 50 && val >= 25) {
      return "Yes";
  }

  // Only change code above this line
  return "No";
}

// Change this value to test
testLogicalAnd(10);
