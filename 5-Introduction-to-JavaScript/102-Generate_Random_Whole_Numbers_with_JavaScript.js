//Learned how to generate random whole numbers with Math.random(). Ex. Math.floor rounds the number down to its nearest whole number, so then you can multiply the function by a
//number to get from one minus that number to 0.
var randomNumberBetween0and19 = Math.floor(Math.random() * 20);

function randomWholeNum() {

  // Only change code below this line.

  return Math.floor(Math.random() * 10);
}
