//Learned to use escape sequences in strings, which can be put into what the program says.
// The code \' = single quote
// The code \" = double quote
// The code \\ = backslash
// The code \n = newline
// The code \r = carriage return
// The code \t = tab
// The code \b = backspace
// The code \f = form feed
var myStr = 'FirstLine\n\t\\SecondLine\nThirdLine'; // Change this line
